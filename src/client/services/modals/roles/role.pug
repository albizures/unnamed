extends ../layout.pug

block body
  .md-inline-form
    +form('form','submitFrom(form)', 'Guardar')
      .row
        .col-xs-12
          +formGroup('name', 'role', 'form', 'text', 'Nombre')
          +formGroup('description', 'role', 'form', 'text', 'Descripcion')
